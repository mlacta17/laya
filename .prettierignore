# Generated byte-for-byte by `pnpm --filter @laya/api types`.
# `wrangler types --check` owns verification; formatting this file would make
# the generated-artifact check fail.
apps/api/worker-configuration.d.ts

# Generated by pnpm.
pnpm-lock.yaml

# Build output and local runtime state.
**/dist/
**/.wrangler/

# Prose documents keep their hand-formatted tables and long lines — the
# ARCHITECTURE.md changelog is the freshness key, not a formatting target.
docs/
