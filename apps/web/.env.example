# Copy to .env (gitignored). Vite exposes only VITE_-prefixed variables to the
# browser — never put secrets here; anything in this file ships to clients.

# Base URL of the Laya API (local Wrangler dev server by default).
VITE_API_URL=http://localhost:8787
