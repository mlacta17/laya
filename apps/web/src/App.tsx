// Placeholder — the /v1/health status page lands later in Phase 0A (scope
// item 6). No styling or design system until Phase 3 (DESIGN.md owns that).
export function App() {
  return <p>Laya web scaffold — health page coming later in Phase 0A.</p>;
}
