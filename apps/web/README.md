# @laya/web

Vite + React + TypeScript SPA — the browser client. Phase 0A ships only a page that calls `GET /v1/health`; appearance is owned by Phase 3 / DESIGN.md.
`pnpm dev` starts it locally; copy `.env.example` to `.env` first so it knows the API URL.
