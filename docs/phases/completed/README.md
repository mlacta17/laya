# Completed phase briefs

Each finished phase brief is archived here with its completion status, commit, outcome, and links to any resulting ADRs.
The active brief remains in `../active/` until its milestone and acceptance checks have passed.
