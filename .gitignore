# Dependencies and build output
node_modules/
dist/
coverage/

# Wrangler local state (local D1, cached builds)
.wrangler/

# Local secrets — never committed (see CLAUDE.md hard rules).
# The committed *.example files document every required variable.
.dev.vars
.dev.vars.*
!.dev.vars.example
.env
.env.*
!.env.example

# Editor/OS noise
.DS_Store
Thumbs.db
